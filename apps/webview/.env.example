# Server-side API base URL. Keep the /api/v1 prefix here so future webview
# requests can reuse the same configuration.
WEBVIEW_API_URL=http://localhost:8080/api/v1
WEBVIEW_PUBLIC_API_URL=http://localhost:8081
